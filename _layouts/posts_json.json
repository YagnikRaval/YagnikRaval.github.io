---
layout: none
---
[
{% for post in site.posts %]
  {
    "title": {{ post.title | jsonify }},
    "url": {{ post.url | relative_url | absolute_url | jsonify }},
    "date": {{ post.date | date: "%Y-%m-%d" | jsonify }},
    "excerpt": {{ post.excerpt | strip_html | truncate: 200 | jsonify }},
    "tags": {{ post.tags | jsonify }}
  }{% if forloop.last %}
  {% else %},{% endif %}
{% endfor %}
]
